# Week1 - Chapter02

---

## 任务1
见同目录下的`AGENTS.md `

## 任务2
生成提示词如下：
```markdown
Write a Python Function: get a specified repository infomation(the number of Stars, the number of Forks, Description) through GitHub API, then save as utils/github_api(_new).py
```
先生成github_api.py，重命名AGENTS.md后，再生成github_api.py

## 任务3
生成提示词如下：
```Markdown
please compare two source code: utils/github_api.py and utils/github_api_new.py. These two code are also generated by AI, github_api.py is generated with Memory, while github_api_new.py is generated without Memory. Create a file name memory_comparison.md and use table to compare the differences in AI-generated code between "with Memory" and "without Memory" scenarios, including five dimentions: naming style, docstring, logging method, error handling, and file location. Please alos write a conclusion.
```

结果见同目录下的：`memory_comparision.md`